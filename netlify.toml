[build]
  command = "npm run build"
  publish = "out"
  functions = "netlify/functions"

# Scheduled function runs every minute to check for notifications
[functions."scheduled-notifications"]
  schedule = "* * * * *"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
